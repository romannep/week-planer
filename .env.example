# Сервер (на чём слушает бэкенд)
PORT=3000
HOST=0.0.0.0

# Путь к файлу SQLite (относительно каталога server или абсолютный)
DATABASE_PATH=data/weekplanner.sqlite

# Клиент: полный URL API (протокол + хост + порт), подставляется в собранный фронт при билде
API_URL=http://localhost:3000
